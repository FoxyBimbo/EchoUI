<Window x:Class="PrismPane_Widgets.Views.SlideshowWidget"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Slideshow" Width="360" Height="280"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent" Topmost="False"
        ShowInTaskbar="False" ResizeMode="CanResize">
    <WindowChrome.WindowChrome>
        <WindowChrome ResizeBorderThickness="8" CaptionHeight="0" GlassFrameThickness="0" CornerRadius="0"/>
    </WindowChrome.WindowChrome>
    <Grid>
        <Border x:Name="RootBorder" Background="{DynamicResource WindowBackgroundSemiBrush}" CornerRadius="14" Margin="8"
                BorderBrush="{DynamicResource AccentBrush}" BorderThickness="1.5"
                MouseLeftButtonDown="RootBorder_MouseLeftButtonDown">
            <Border.Effect>
                <DropShadowEffect BlurRadius="16" ShadowDepth="2" Opacity="0.5" Color="Black"/>
            </Border.Effect>
            <Grid x:Name="InnerLayout" Margin="12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <DockPanel x:Name="HeaderPanel" Grid.Row="0" Margin="0,0,0,6">
                    <Button Content="✕" Click="BtnClose_Click" DockPanel.Dock="Right"
                            Background="Transparent" Foreground="{DynamicResource MutedForegroundBrush}" BorderBrush="Transparent"
                            FontSize="14" Padding="4,0" Cursor="Hand" MinWidth="24" ToolTip="Close"/>
                    <Button x:Name="BtnMinimize" Content="—" DockPanel.Dock="Right"
                            Background="Transparent" Foreground="{DynamicResource MutedForegroundBrush}" BorderBrush="Transparent"
                            FontSize="14" Padding="4,0" Cursor="Hand" MinWidth="24" ToolTip="Minimize"/>
                    <Button Content="⚙" Click="BtnSettings_Click" DockPanel.Dock="Right"
                            Background="Transparent" Foreground="{DynamicResource MutedForegroundBrush}" BorderBrush="Transparent"
                            FontSize="14" Padding="4,0" Cursor="Hand" MinWidth="24" ToolTip="Settings"/>
                    <TextBlock x:Name="TxtWidgetTitle" Text="Slideshow" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource ForegroundBrush}"/>
                </DockPanel>

                <Grid x:Name="ContentPanel" Grid.Row="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="{DynamicResource ControlBackgroundBrush}" CornerRadius="8"
                            BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" ClipToBounds="True">
                        <Grid>
                            <Image x:Name="ImgSlideshow" Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality"/>
                            <TextBlock x:Name="TxtNoImages" Text="No images found. Open settings to select a folder."
                                       HorizontalAlignment="Center" VerticalAlignment="Center" TextWrapping="Wrap"
                                       Foreground="{DynamicResource MutedForegroundBrush}" TextAlignment="Center" Margin="12"/>
                        </Grid>
                    </Border>

                    <DockPanel Grid.Row="1" Margin="0,6,0,0">
                        <Button Content="◀" Click="BtnPrevious_Click" DockPanel.Dock="Left"
                                Background="Transparent" Foreground="{DynamicResource MutedForegroundBrush}" BorderBrush="Transparent"
                                FontSize="14" Padding="6,2" Cursor="Hand" ToolTip="Previous"/>
                        <Button Content="▶" Click="BtnNext_Click" DockPanel.Dock="Right"
                                Background="Transparent" Foreground="{DynamicResource MutedForegroundBrush}" BorderBrush="Transparent"
                                FontSize="14" Padding="6,2" Cursor="Hand" ToolTip="Next"/>
                        <TextBlock x:Name="TxtImageInfo" Text="" HorizontalAlignment="Center" VerticalAlignment="Center"
                                   Foreground="{DynamicResource MutedForegroundBrush}" FontSize="11"/>
                    </DockPanel>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>
