<Window x:Class="PrismPane_Widgets.Views.WidgetGroupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Widget Group" Width="300" Height="300"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent" Topmost="False"
        ShowInTaskbar="False" ResizeMode="CanResize">
    <WindowChrome.WindowChrome>
        <WindowChrome ResizeBorderThickness="8" CaptionHeight="0" GlassFrameThickness="0" CornerRadius="0"/>
    </WindowChrome.WindowChrome>
    <Grid>
        <Border x:Name="RootBorder" Background="{DynamicResource WindowBackgroundSemiBrush}" CornerRadius="14" Margin="8"
                BorderBrush="{DynamicResource AccentBrush}" BorderThickness="1.5"
                MouseLeftButtonDown="RootBorder_MouseLeftButtonDown">
            <Border.Effect>
                <DropShadowEffect BlurRadius="16" ShadowDepth="2" Opacity="0.5" Color="Black"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Tab strip row: drag handle + tabs + close -->
                <Grid Grid.Row="0" Margin="0,6,0,2" Background="Transparent">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Drag grip ─ always visible, always receives mouse -->
                    <Border Grid.Column="0" Width="28" Cursor="SizeAll" Background="Transparent"
                            MouseLeftButtonDown="DragGrip_MouseLeftButtonDown">
                        <TextBlock Text="⠿" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"
                                   Foreground="{DynamicResource MutedForegroundBrush}" IsHitTestVisible="False"/>
                    </Border>

                    <!-- Tab buttons -->
                    <ScrollViewer x:Name="TabScrollViewer" Grid.Column="1"
                                  HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Disabled"
                                  Padding="0" Margin="0" Background="Transparent"
                                  PreviewMouseLeftButtonDown="TabArea_PreviewMouseLeftButtonDown">
                        <StackPanel x:Name="TabStrip" Orientation="Horizontal"/>
                    </ScrollViewer>

                    <!-- Minimize + Ungroup buttons -->
                    <StackPanel x:Name="ButtonsPanel" Grid.Column="2" Orientation="Horizontal">
                        <Button x:Name="BtnMinimize" Content="—" Click="BtnMinimize_Click"
                                Background="Transparent" Foreground="{DynamicResource MutedForegroundBrush}" BorderBrush="Transparent"
                                FontSize="14" Padding="4,0" Cursor="Hand" MinWidth="24" ToolTip="Minimize"/>
                        <Button Content="✕" Click="BtnCloseGroup_Click"
                                Background="Transparent" Foreground="{DynamicResource MutedForegroundBrush}" BorderBrush="Transparent"
                                FontSize="14" Padding="4,0" Margin="0,0,8,0" Cursor="Hand" MinWidth="24" ToolTip="Ungroup All"/>
                    </StackPanel>
                </Grid>

                <!-- Active tab content -->
                <Border x:Name="ContentBorder" Grid.Row="1" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,0">
                    <ContentPresenter x:Name="ContentHost"/>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Window>
