<Window x:Class="PrismPane_Widgets.Views.GpuMonitorWidget"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="GPU Monitor" Width="260" Height="220"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent" Topmost="False"
        ShowInTaskbar="False" ResizeMode="CanResize">
    <WindowChrome.WindowChrome>
        <WindowChrome ResizeBorderThickness="8" CaptionHeight="0" GlassFrameThickness="0" CornerRadius="0"/>
    </WindowChrome.WindowChrome>
    <Grid>
        <Border x:Name="RootBorder" Background="{DynamicResource WindowBackgroundSemiBrush}" CornerRadius="14" Margin="8"
                BorderBrush="{DynamicResource AccentBrush}" BorderThickness="1.5"
                MouseLeftButtonDown="RootBorder_MouseLeftButtonDown">
            <Border.Effect>
                <DropShadowEffect BlurRadius="16" ShadowDepth="2" Opacity="0.5" Color="Black"/>
            </Border.Effect>
            <Grid x:Name="InnerLayout" Margin="12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <DockPanel x:Name="HeaderPanel" Grid.Row="0" Margin="0,0,0,6">
                    <Button Content="✕" Click="BtnClose_Click" DockPanel.Dock="Right"
                            Background="Transparent" Foreground="{DynamicResource MutedForegroundBrush}" BorderBrush="Transparent"
                            FontSize="14" Padding="4,0" Cursor="Hand" MinWidth="24" ToolTip="Close"/>
                    <Button x:Name="BtnMinimize" Content="—" DockPanel.Dock="Right"
                            Background="Transparent" Foreground="{DynamicResource MutedForegroundBrush}" BorderBrush="Transparent"
                            FontSize="14" Padding="4,0" Cursor="Hand" MinWidth="24" ToolTip="Minimize"/>
                    <Button Content="⚙" Click="BtnSettings_Click" DockPanel.Dock="Right"
                            Background="Transparent" Foreground="{DynamicResource MutedForegroundBrush}" BorderBrush="Transparent"
                            FontSize="14" Padding="4,0" Cursor="Hand" MinWidth="24" ToolTip="Settings"/>
                    <TextBlock x:Name="TxtWidgetTitle" Text="GPU" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource ForegroundBrush}"/>
                </DockPanel>

                <Grid x:Name="ContentPanel" Grid.Row="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock x:Name="TxtUsage" Grid.Row="0" FontSize="24" FontWeight="SemiBold"
                               Foreground="{DynamicResource ForegroundBrush}" HorizontalAlignment="Center" Margin="0,0,0,8"
                               Text="0%"/>

                    <Grid x:Name="BarPanel" Grid.Row="1" Visibility="Visible" Margin="6,0,6,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Border x:Name="UsageBarTrack" Grid.Row="0" Height="20" CornerRadius="10"
                                Background="{DynamicResource ControlBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                                VerticalAlignment="Center">
                            <Border x:Name="UsageBarFill" HorizontalAlignment="Left" Height="18" Width="0" CornerRadius="9"/>
                        </Border>

                        <TextBlock x:Name="TxtBarLabel" Grid.Row="1" Margin="0,8,0,0" Text="Usage bar"
                                   HorizontalAlignment="Center" Foreground="{DynamicResource MutedForegroundBrush}"/>
                    </Grid>

                    <WrapPanel Grid.Row="2" Margin="0,8,0,0" HorizontalAlignment="Center">
                        <StackPanel Orientation="Horizontal" Margin="0,0,8,0">
                            <Ellipse x:Name="LowIndicator" Width="10" Height="10" Margin="0,0,4,0" Stroke="{DynamicResource BorderBrush}" StrokeThickness="1"/>
                            <TextBlock Text="Low" Foreground="{DynamicResource MutedForegroundBrush}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,8,0">
                            <Ellipse x:Name="MediumIndicator" Width="10" Height="10" Margin="0,0,4,0" Stroke="{DynamicResource BorderBrush}" StrokeThickness="1"/>
                            <TextBlock Text="Medium" Foreground="{DynamicResource MutedForegroundBrush}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <Ellipse x:Name="HighIndicator" Width="10" Height="10" Margin="0,0,4,0" Stroke="{DynamicResource BorderBrush}" StrokeThickness="1"/>
                            <TextBlock Text="High" Foreground="{DynamicResource MutedForegroundBrush}"/>
                        </StackPanel>
                    </WrapPanel>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>
