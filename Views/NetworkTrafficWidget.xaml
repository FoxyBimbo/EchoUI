<Window x:Class="PrismPane_Widgets.Views.NetworkTrafficWidget"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Network Traffic" Width="260" Height="220"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent" Topmost="False"
        ShowInTaskbar="False" ResizeMode="CanResize">
    <WindowChrome.WindowChrome>
        <WindowChrome ResizeBorderThickness="8" CaptionHeight="0" GlassFrameThickness="0" CornerRadius="0"/>
    </WindowChrome.WindowChrome>
    <Grid>
        <Border x:Name="RootBorder" Background="{DynamicResource WindowBackgroundSemiBrush}" CornerRadius="14" Margin="8"
                BorderBrush="{DynamicResource AccentBrush}" BorderThickness="1.5"
                MouseLeftButtonDown="RootBorder_MouseLeftButtonDown">
            <Border.Effect>
                <DropShadowEffect BlurRadius="16" ShadowDepth="2" Opacity="0.5" Color="Black"/>
            </Border.Effect>
            <Grid x:Name="InnerLayout" Margin="12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <DockPanel x:Name="HeaderPanel" Grid.Row="0" Margin="0,0,0,6">
                    <Button Content="✕" Click="BtnClose_Click" DockPanel.Dock="Right"
                            Background="Transparent" Foreground="{DynamicResource MutedForegroundBrush}" BorderBrush="Transparent"
                            FontSize="14" Padding="4,0" Cursor="Hand" MinWidth="24" ToolTip="Close"/>
                    <Button x:Name="BtnMinimize" Content="—" DockPanel.Dock="Right"
                            Background="Transparent" Foreground="{DynamicResource MutedForegroundBrush}" BorderBrush="Transparent"
                            FontSize="14" Padding="4,0" Cursor="Hand" MinWidth="24" ToolTip="Minimize"/>
                    <Button Content="⚙" Click="BtnSettings_Click" DockPanel.Dock="Right"
                            Background="Transparent" Foreground="{DynamicResource MutedForegroundBrush}" BorderBrush="Transparent"
                            FontSize="14" Padding="4,0" Cursor="Hand" MinWidth="24" ToolTip="Settings"/>
                    <TextBlock x:Name="TxtWidgetTitle" Text="Network" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource ForegroundBrush}"/>
                </DockPanel>

                <Grid x:Name="ContentPanel" Grid.Row="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,4">
                        <TextBlock x:Name="TxtDownloadSpeed" FontSize="18" FontWeight="SemiBold"
                                   Foreground="{DynamicResource ForegroundBrush}" HorizontalAlignment="Center"
                                   Text="↓ 0 B/s"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" HorizontalAlignment="Center" Margin="0,0,0,8">
                        <TextBlock x:Name="TxtUploadSpeed" FontSize="18" FontWeight="SemiBold"
                                   Foreground="{DynamicResource ForegroundBrush}" HorizontalAlignment="Center"
                                   Text="↑ 0 B/s"/>
                    </StackPanel>

                    <Grid Grid.Row="2" Margin="6,0,6,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="8"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Border x:Name="DownloadBarTrack" Grid.Row="0" Height="14" CornerRadius="7"
                                Background="{DynamicResource ControlBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                                VerticalAlignment="Center">
                            <Border x:Name="DownloadBarFill" HorizontalAlignment="Left" Height="12" Width="0" CornerRadius="6"/>
                        </Border>

                        <Border x:Name="UploadBarTrack" Grid.Row="2" Height="14" CornerRadius="7"
                                Background="{DynamicResource ControlBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                                VerticalAlignment="Center">
                            <Border x:Name="UploadBarFill" HorizontalAlignment="Left" Height="12" Width="0" CornerRadius="6"/>
                        </Border>

                        <WrapPanel Grid.Row="3" Margin="0,8,0,0" HorizontalAlignment="Center" VerticalAlignment="Bottom">
                            <StackPanel Orientation="Horizontal" Margin="0,0,8,0">
                                <Ellipse x:Name="DownloadIndicator" Width="10" Height="10" Margin="0,0,4,0" Stroke="{DynamicResource BorderBrush}" StrokeThickness="1"/>
                                <TextBlock Text="Down" Foreground="{DynamicResource MutedForegroundBrush}"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <Ellipse x:Name="UploadIndicator" Width="10" Height="10" Margin="0,0,4,0" Stroke="{DynamicResource BorderBrush}" StrokeThickness="1"/>
                                <TextBlock Text="Up" Foreground="{DynamicResource MutedForegroundBrush}"/>
                            </StackPanel>
                        </WrapPanel>
                    </Grid>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>
